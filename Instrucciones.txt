Instrucciones para Ejecutar:


1- Clonar el repositorio con el comando:

git clone https://github.com/tu_usuario/tu_repositorio.git
cd repositorio



2- Construir la imagen de Docker con el siguiente comando:

docker build -t airflow_spotify .



3- Iniciar los servicios con Docker Compose usando el siguiente comando:

docker-compose up -d



4- Acceder a la Interfaz de Airflow

Abre tu navegador y ve a http://localhost:8080. Inicia sesión con las credenciales admin/admin.